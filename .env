# URL pública do backend para Expo Go em celular físico, web e demonstração.
# Use HTTPS em qualquer ambiente que não seja o emulador local.
EXPO_PUBLIC_API_URL=https://advisors-considerations-period-offset.trycloudflare.com/api/v1/
